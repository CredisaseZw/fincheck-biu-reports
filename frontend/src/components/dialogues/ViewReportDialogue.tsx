import { Button } from "../ui/button";
import { Dialog, DialogTrigger } from "../ui/dialog";

function ViewReportDialogue() {
  return (
    <Dialog>
      <DialogTrigger>
        <Button>View Report </Button>
      </DialogTrigger>
    </Dialog>
  )
}

export default ViewReportDialogue